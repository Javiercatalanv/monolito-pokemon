# Dependencias exclusivas de desarrollo y CI.
# Instalar con: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

# Testing
pytest>=8.1.0
pytest-cov>=5.0.0
httpx>=0.27.0          # requerido por fastapi.testclient.TestClient

# Calidad de codigo
ruff>=0.4.0
black>=24.3.0
